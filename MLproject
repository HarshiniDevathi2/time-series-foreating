name: time_Series_forecasting

entry_points:
  main:
    parameters:
      dataset_no: {type: integer, default: 1}
      testno: {type: integer, default: 20}
    command: "Rscript.exe time_Series_forecasting.R {dataset_no} {testno}"
